apiVersion: v1
kind: Service 
metadata:
  - name: ngixns
spec:
  type: ClusterIP
  clusterIP: none
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetport: 80